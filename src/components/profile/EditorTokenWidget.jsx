import { getUserTokenUsage } from '../../services/api';
import './EditorTokenWidget.css';

/**
 * Function that was previously used to add token widget to editor
 * This functionality has been removed and token usage is now only shown in the subscription section
 */
export const addTokenWidgetToEditor = async () => {
  // Token widget has been removed from editor stats
  console.log('Token widget functionality has been moved to subscription section');
  return;
}; 